#include <stdio.h>
#include "main.h"  // Include the header file

/* main function */
int main(int argc, char *argv[])
{
    /* Print the name of the program (argv[0]) */
    printf("%s\n", argv[0]);

    return 0;
}
